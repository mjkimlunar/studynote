# StudyNote — 공부 노트 → 블로그 초안 에이전트 PRD

작성 2026-09-09 · v0.1 (초안 — 검토·수정 필요)

---

## 1. 문제 정의

공부하면서 쌓이는 노트(책 요약, 사진으로 찍은 페이지, 강의 메모 등)는 그 자체로는 남에게
보여주기 애매한 형태다 — 발제용으로는 너무 상세하고, 그대로 블로그에 올리기엔 문체가 딱딱하거나
정리가 안 되어 있다. 매번 "이 노트를 블로그 톤으로 다시 써줘"를 수작업으로 반복하는 게 이 에이전트가
대신 처리할 반복 업무다.

**입력** — 텍스트 노트(요약·정리글) 또는 책/문서 사진 1~N장.
**출력** — (선택적으로 관련 최신 자료가 보강된) 블로그 초안 1편 — 본인의 문체를 반영한 상태로,
사람이 검토 후 바로 발행할 수 있는 수준.

## 2. 타겟 유저

| | 대상 | 왜 |
| --- | --- | --- |
| 1순위 | **작성자 본인** | 책 공부 노트(오늘 한 것과 같은 작업)를 블로그로 발행하고 싶은 사람 |
| 2순위 | 같은 스터디 팀원 | 관심사가 다양해서(ISMS, 일반 IT, 여행 등) 특정 도메인에 매이지 않는 범용 노트→블로그 도구가 필요 |

두 사용자 모두 "노트를 대신 완성해 주는 것"이 목적이지, 노트 내용 자체의 사실 검증을 대행하는 게
목적이 아니다.

**모든 사용자가 기존 블로그를 갖고 있는 건 아니다.** 또한 같은 사람이라도 공식적인 말투와
일상 어투가 다를 수 있다 — 그래서 스타일 소스는 "기존 글 조회"뿐 아니라 "짧은 샘플을 새로
작성받아 분석"하는 경로도 함께 지원한다 (§5 글 저장소 조회 도구 참고).

## 3. 목표 / 비목표

**목표**
- 노트(텍스트/사진)를 받아서 구조화된 블로그 초안을 생성한다.
- 원하면 노트 주제와 관련된 최신 자료를 검색해서 보강한다.
- 본인의 과거 글 스타일을 반영해서, "AI가 쓴 티"가 덜 나는 톤으로 재작성한다.
- 발행 전 반드시 사람이 초안을 확인·수정할 수 있게 한다.

**비목표**
- 검색 도구는 관련 자료를 찾아 **출처와 함께 첨부**할 뿐, 그 자료로 노트 내용의 진위를
  판정하거나 노트-자료 간 모순을 자동으로 지적하지 않는다("확인 필요"까지만, 판단은 사람이).
- **완전 자동 발행(사람 승인 없이 공개)은 하지 않는다** — 승인 전까지는 항상 초안(draft) 상태.
- 여러 플랫폼 동시 발행, 다국어 번역, SEO 최적화는 이번 범위 밖.

## 3.1 공개 안전성 원칙 (저작권)

개인이 혼자 보는 노트와 **블로그로 공개 발행하는 것은 위험도가 다르다.** 발행 전 콘텐츠를
세 종류로 나눠서 다룬다.

| 구분 | 예시 | 발행 가능 여부 |
| --- | --- | --- |
| **공개 소스 링크** | arXiv 논문, 위키피디아, 깃허브, 공개 블로그 URL | ✅ 그대로 인용/링크 가능 (인용 자체가 목적인 자료) |
| **본인 재구성** | 책 내용을 자기 말로 요약, 그림을 텍스트/다이어그램으로 다시 그린 것 + 짧은 원문 인용(출처 명시) | ✅ 발행 가능 — **원칙: 원저작물 그대로 복제 금지, 재가공 + 출처 명시는 가능** |
| **원저작물 직접 발췌** | 책 페이지 사진, 긴 원문 그대로, 표/그림 이미지 원본 | ⚠️ **발행 도구 입력에서 자동 제외** — "다시 써야 함"으로 표시 |

**검색/RAG 도구는 인용 실재성도 확인한다** — 기존 `citecheck`(MisMatch) 프로젝트의 원칙을
그대로 가져온다: **"실재하는지만 확인, 진위는 판정하지 않는다."** Crossref·arXiv 등으로 존재
확인 후 링크만 첨부 → LLM이 없는 논문을 지어내는(할루시네이션) 문제를 원천 차단.

## 4. 워크플로 설계

```
[트리거] 사용자가 노트(텍스트 또는 이미지 1~N장)를 입력
   │
   ▼
[단계1: 파싱] (도구가 처리) 텍스트 추출/정리 — 이미지면 OCR·구조화
   │
   ▼
[단계2: 보강 여부 판단] (에이전트가 판단) 노트에 "최신 정보가 필요한 주장"이 있는가?
   │                     있으면 → 단계3, 없으면 → 단계4로 바로
   ▼
[단계3: 검색/RAG] (도구가 처리) 관련 최신 자료 검색 → 참고자료로 첨부
   │
   ▼
[단계4: 구조화] (에이전트가 판단) 노트에서 핵심 주장 2~3개, 글의 뼈대를 추출
   │
   ▼
[단계5: 스타일 변환] (에이전트가 판단 + LLM 호출) 본인 과거 글(도구로 조회)을 예시로 활용해
   │                  블로그 톤으로 재작성
   ▼
[단계6: 사람 개입] ★ 초안을 화면에 보여주고 승인/수정 요청 — 승인 전엔 아무것도 발행 안 됨
   │
   ├─ 수정 요청 → 단계5로 되돌아가 재작성
   ▼
[단계7: 발행] (도구가 처리, 승인 후에만 실행) 지정 플랫폼에 초안으로 게시
```

## 5. 도구 계획

| 도구 | 역할 | 입력 | 출력 | 권한 |
| --- | --- | --- | --- | --- |
| **검색/RAG 도구** | 노트 주제 관련 최신 자료 검색 + **인용 실재성 확인** | 검색 쿼리(문자열) | 문서 스니펫 목록(제목·URL·요약) + 실재성 확인 결과 | 읽기 전용 — 외부 API 호출만, 아무것도 안 씀 |
| **글 저장소 조회 도구** | 스타일 예시 조회 — **경로 A**: 기존 블로그/글 저장소에서 조회. **경로 B**(기존 글이 없는 사용자용): 온보딩 시 짧은 노트/반응 2~3개를 직접 작성받아 그것을 예시로 사용 | 없음 또는 태그 | 스타일 예시 텍스트 N개 | 읽기 전용 — 로컬 파일/DB 읽기만 |
| **발행 도구** | 블로그 플랫폼에 초안 게시 | 완성된 초안(제목+본문) | 게시된 초안의 URL | **쓰기 권한 — 반드시 "초안(draft)" 상태로만 생성**, 실제 공개(publish)는 별도 사람 조작 |

**실패 처리 규칙**
- 검색 도구 실패/결과 없음 → 보강 없이 원래 노트만으로 진행 (중단하지 않음)
- 발행 도구 실패 → 최대 2회 재시도 → 그래도 실패하면 로컬 파일로 초안 저장 + 사람에게 알림

## 6. 사람 개입 지점 & 관찰 가능성

- **필수 승인 지점**: 단계6(발행 전 초안 검토) — 이 단계를 건너뛰고 발행되는 경로는 없음.
- **실행 로그**: 각 실행마다 "어느 도구를 왜 호출했는지, 무엇을 받았는지"를 화면에서 확인 가능해야
  함 (검색 쿼리·검색 결과 요약·최종 프롬프트 등).
- **비용/시간 기록**: 실행별 소요시간·토큰수를 기록해 병목 단계 파악.

## 7. 핵심 기능(MVP) & 화면 구성

**MVP 기능**
1. 노트 입력(텍스트 붙여넣기 + 이미지 업로드)
2. 실행 → 진행 상황(단계별 로그) 실시간 표시
3. 초안 미리보기 + 승인/수정 버튼
4. 승인 시 발행(초안 상태)

**화면**
- 입력 화면 (텍스트/이미지 업로드)
- 실행 로그 화면 (단계별 트레이스, 소요시간/토큰)
- 초안 검토 화면 (미리보기 + 승인/수정/재생성 버튼)

## 8. 실험 & 평가 (초안 — 실제 노트로 채울 것)

- **평가셋**: 오늘 만든 책 공부 노트(2.1~3장) 중 5~10개를 입력으로 사용
- **측정 기준**: 완료율(끝까지 도는가), 본인 문체 반영도(주관 평가 1~5점), 소요시간, 토큰비용
- **실패 유형 기록**: 검색 결과가 노트와 무관, 스타일 변환이 과도해서 원문 의미 왜곡, 무한 재시도 등
- **토픽 전이 실험 (첫 실험 케이스)**: 일상글(여행·일상) 3개로 학습한 스타일이 정보전달형
  공부노트에 적용했을 때도 자연스러운지, 아니면 너무 가벼워 보이는지 확인. 안 맞으면
  "공부노트 전용 스타일 예시"를 별도로 추가 수집.

## 9. 배포 계획

- 형태: 웹앱 (입력→실행→검토 3화면)
- 배포처: (미정 — GitHub Pages는 정적이라 서버 로직 필요시 Vercel/Cloudflare Workers 등 검토)
- 수용 기준(초안): 대표 시나리오 5개 중 사람 개입 없이 초안까지 도달한 것이 4개 이상

---

## 남은 결정 필요 사항 (▢)

- [ ] 발행 플랫폼 확정 (깃허브 Pages? 노션? 벨로그?) — API 권한 설계가 여기 달림
- [ ] 에이전트 프레임워크 (직접 구현 vs LangGraph vs Claude Agent SDK vs n8n)
- [x] 검색 도구 — `citecheck`(MisMatch)의 실재성 확인 로직(Crossref·arXiv 등) 재사용으로 결정
- [x] "본인 과거 글" 소스 — 네이버 블로그(honeysdream) 확정, 단 **선별 기준 있음** (아래 3.2)

## 3.2 스타일 예시 선별 기준 (블로그 실사례 분석 결과)

블로그 글 몇 개를 직접 대조해본 결과, **문체가 두 갈래로 나뉘어 있었다** — 원인은 개인 취향
차이가 아니라 **AI 협업 여부**였다 (유입을 늘리려고 최근 일부 글을 AI와 합작).

| 구분 | 특징 | 스타일 예시로 쓸지 |
| --- | --- | --- |
| **AI 협업 이전 (반말체)** | 짧고 끊어지는 문장, 말줄임표, ㅋㅋ·ㅎㅎ·ㅠㅠ, 정보 사이사이 본인 반응/의견을 짧게 섞음 | ✅ **기준 문체로 채택** — 순도 100% 본인 목소리 |
| **AI 협업 이후 (존댓말+구조화)** | 번호·카테고리로 정리된 완결된 정보 나열, 존댓말, 다소 홍보문구적인 마무리 | ❌ **제외** — 역중립화 원리상, AI가 섞인 글을 "본인 목소리"로 학습시키면 순환 오류 발생 |

**우선순위 원칙**: 장르(정보전달형 vs 일기형) 일치보다 **목소리 순도(AI 혼입 여부)**가 우선.
StudyNote 결과물은 "정보전달형 내용을 반말체 개인 목소리로 전달"하는 방향이 된다 —
정보전달인데 존댓말이 아니라서 어색해 보일 수 있으나, 오히려 AI 티 안 나는 차별점.

**본인 목소리의 핵심 특징 (장르 무관하게 공통)**
- 짧고 끊어지는 문장, 완결된 문장보다 단상 나열에 가까움
- 말줄임표(..)로 끝맺는 습관
- ㅋㅋ·ㅎㅎ·ㅠㅠ 등 감정표현이 문장 끝에 자주 붙음
- **정보 사이사이 본인의 짧은 반응·감상·판단을 자연스럽게 끼워넣음** (순수 정보나열과 구분되는
  가장 중요한 특징)